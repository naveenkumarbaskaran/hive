# Task Completion Checklist

1. `make lint` — ruff check must pass
2. `make fmt` — auto-format
3. `make test` — 715+ tests must pass
4. Keep runtime deps minimal (httpx only) — check before adding anything
